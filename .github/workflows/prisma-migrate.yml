name: Prisma build and migrate

on:
  push:
    branches: [main]
    paths:
      - "services/api/prisma/**"
      - "services/api/src/**"
      - "services/api/package.json"
      - "packages/db/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/prisma-migrate.yml"
  pull_request:
    paths:
      - "services/api/prisma/**"
      - "services/api/src/**"
      - "services/api/package.json"
      - "packages/db/**"
      - "package.json"
      - "package-lock.json"
      - ".github/workflows/prisma-migrate.yml"
  workflow_dispatch:

concurrency:
  group: prisma-migrate-${{ github.ref }}
  cancel-in-progress: false

jobs:
  prisma:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: dialective
          POSTGRES_PASSWORD: dialective
          POSTGRES_DB: dialective
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U dialective -d dialective"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://dialective:dialective@localhost:5432/dialective
      SHADOW_DATABASE_URL: postgresql://dialective:dialective@localhost:5432/dialective_shadow

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client and build @dialectiva/db
        run: npm run prisma:generate --workspace=@dialectiva/api && npm run build --workspace=@dialectiva/db

      - name: Build API
        run: npm run build:app --workspace=@dialectiva/api

      - name: Create shadow database
        run: PGPASSWORD=dialective psql -h localhost -U dialective -d dialective -c "CREATE DATABASE dialective_shadow;"

      - name: Apply migrations
        run: npm run prisma:deploy --workspace=@dialectiva/api

      - name: Verify production seed
        run: npm run prisma:seed --workspace=@dialectiva/api

      - name: Verify schema is in sync with migrations
        working-directory: services/api
        run: npx prisma migrate diff --from-migrations prisma/migrations --to-schema prisma/schema.prisma --exit-code

