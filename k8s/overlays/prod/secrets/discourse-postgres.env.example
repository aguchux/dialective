# Copy to discourse-postgres.env (gitignored) and fill in real values.
# Backs the discourse-postgres-creds k8s Secret. Separate Postgres instance
# from the main app's postgres-creds -- Discourse owns its own database.
username=discourse
password=changeme
