# Copy to postgres.env (gitignored) and fill in real values.
# Backs the postgres-creds k8s Secret.
username=dialectiva
password=changeme
connection_string=postgresql://dialectiva:changeme@postgres:5432/dialectiva
