# Copy to rabbitmq.env (gitignored). SAME values as DO's
# ../../../overlays/prod/secrets/rabbitmq.env -- GCP's api connects to the
# SAME RabbitMQ instance DO runs (over rabbitmq-external's LoadBalancer,
# see ../configs/api.env.example's rabbitmq_host), not a separate broker,
# so this must be the real account, not a placeholder.
username=dialectiva-rabbitmq-admin
password=changeme
