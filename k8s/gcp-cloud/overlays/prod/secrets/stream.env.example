# Copy to stream.env (gitignored). SAME value as DO's
# ../../../overlays/prod/secrets/stream.env -- must be the exact same
# secret, since a Voice Stream subscriber token minted by one cluster's
# api pod must validate on requests the other cluster's pod handles.
stream_jwt_access_secret=changeme
