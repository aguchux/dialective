# Copy to llm.env (gitignored). SAME values as DO's
# ../../../overlays/prod/secrets/llm.env -- one shared set of LLM provider
# keys, not per-cluster ones.
openai_api_key=changeme
deepseek_api_key=changeme
anthropic_api_key=changeme
