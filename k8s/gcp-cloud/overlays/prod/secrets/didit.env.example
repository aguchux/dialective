# Copy to didit.env (gitignored). SAME values as DO's
# ../../../overlays/prod/secrets/didit.env -- one shared set of Didit
# credentials, not per-cluster ones.
didit_api_key=changeme
didit_workflow_id=changeme
didit_webhook_secret=changeme
