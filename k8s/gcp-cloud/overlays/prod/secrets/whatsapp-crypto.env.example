# Copy to whatsapp-crypto.env (gitignored). SAME value as DO's
# ../../../overlays/prod/secrets/whatsapp-crypto.env -- must be the exact
# same key, not a freshly generated one, or ciphertext encrypted by one
# cluster's api pod can't be decrypted by the other's.
whatsapp_settings_encryption_key=changeme
