# Copy to spaces.env (gitignored) and fill in the SAME values as
# ../../../overlays/prod/secrets/spaces.env on the DO side -- DigitalOcean
# Spaces is one shared object store both clusters' vosk-worker read/write
# against, not a per-cluster resource.
endpoint=https://sfo3.digitaloceanspaces.com
access_key=changeme
secret_key=changeme
