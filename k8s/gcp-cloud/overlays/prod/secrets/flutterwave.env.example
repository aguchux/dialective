# Copy to flutterwave.env (gitignored). SAME values as DO's
# ../../../overlays/prod/secrets/flutterwave.env -- one shared set of
# Flutterwave v3 credentials, not per-cluster ones.
flutterwave_secret_key=changeme
flutterwave_public_key=changeme
flutterwave_webhook_secret_hash=changeme
flutterwave_environment=sandbox
