# Copy to hf.env (gitignored) and fill in the real value.
# Backs the hf-creds k8s Secret, consumed by whisper-worker as HF_TOKEN.
# Same token as ../../../overlays/prod/secrets/hf.env -- see that file's
# comment for why a gated-repo HF token is required at all.
hf_token=changeme
